// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2024-2026 Open Android Auto contributors
// Based on works by: f1x.studio (aasdk), opencardev (crankshaft), aa-proxy (aa-proxy-rs)
syntax="proto2";

package oaa.proto.data;

// confidence: silver [apk_static, cross_version]
message AudioConfig
{
    required uint32 sample_rate = 1; // 16.2 vvb / 17.3 xfb RawMessageInfo both mark required  // confidence: silver [apk_static, cross_version]
    required uint32 bit_depth = 2;  // confidence: silver [apk_static, cross_version]
    required uint32 channel_count = 3;  // confidence: silver [apk_static, cross_version]
}
