// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2024-2026 Open Android Auto contributors
// Based on works by: f1x.studio (aasdk), opencardev (crankshaft), aa-proxy (aa-proxy-rs)

/*
 * Audio focus state message (msg ID 0x8021).
 *
 * Simple boolean indicating whether audio focus is currently held.
 * Distinct from AudioFocusState enum (which enumerates focus types
 * like GAIN, LOSS, etc.) — this is a binary focus indicator.
 *
 * APK class: waz (AA v16.1)
 * Field numbers decoded from protobuf-lite RawMessageInfo descriptor.
 */
syntax="proto2";

package oaa.proto.messages;

// confidence: silver [apk_static, cross_version]
message AudioFocusState
{
    optional bool has_focus = 1;  // confidence: silver [apk_static, cross_version]
}
