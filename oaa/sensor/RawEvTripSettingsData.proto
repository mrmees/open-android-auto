// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2024-2026 Open Android Auto contributors
// Based on works by: f1x.studio (aasdk), opencardev (crankshaft), aa-proxy (aa-proxy-rs)

/*
 * Raw EV trip settings data (sensor type 26 = RAW_EV_TRIP_SETTINGS).
 *
 * Opaque blob — internal structure TBD from wire captures.
 *
 * APK class: wbf (AA v16.1), wav (AA v16.2)
 * NOTE: wbf in 16.2 is SensorError — obfuscated name reuse!
 * Field numbers decoded from protobuf-lite RawMessageInfo descriptor.
 */
syntax="proto2";

package oaa.proto.data;

// confidence: silver [apk_static, cross_version]
message RawEvTripSettings
{
    optional bytes raw_data = 1;  // confidence: silver [apk_static, cross_version]
}
