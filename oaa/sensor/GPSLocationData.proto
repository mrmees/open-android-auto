// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2024-2026 Open Android Auto contributors
// Based on works by: f1x.studio (aasdk), opencardev (crankshaft), aa-proxy (aa-proxy-rs)
syntax="proto2";

package oaa.proto.data;

// APK class: vyl (AA v16.1), vxx (AA v16.2) — GPS location data (6 fields, fields 2-7)
// NOTE: No field 1 (timestamp) in APK. Fields 8-22 belong to ahdz, not vxx.
// confidence: silver [apk_static, cross_version]
message GPSLocation
{
    required int32 latitude = 2;  // confidence: silver [apk_static, cross_version]
    required int32 longitude = 3;  // confidence: silver [apk_static, cross_version]
    optional uint32 accuracy = 4;  // confidence: silver [apk_static, cross_version]
    optional int32 altitude = 5;  // confidence: silver [apk_static, cross_version]
    optional int32 speed = 6;  // confidence: silver [apk_static, cross_version]
    optional int32 bearing = 7;  // confidence: silver [apk_static, cross_version]
}
