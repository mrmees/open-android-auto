// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2024-2026 Open Android Auto contributors
// Based on works by: f1x.studio (aasdk), opencardev (crankshaft), aa-proxy (aa-proxy-rs)

/*
 * Sensor channel message IDs.
 *
 * Verified from ibi.java (16.2) handler constants:
 * f36527h=0x8001, f36526g=0x8002, f36528i=0x8003, f36529j=0x8004
 */
syntax="proto3";

package oaa.proto.ids;

// confidence: gold [deep_trace, 16.2 handler ibi.java]
message SensorChannelMessage
{
    // confidence: gold
    enum Enum
    {
        NONE = 0x0000;
        SENSOR_REQUEST = 0x8001;         // HU->Phone, wbh (16.2)
        SENSOR_START_RESPONSE = 0x8002;  // Phone->HU, wbi (16.2)
        SENSOR_EVENT_INDICATION = 0x8003;// Phone->HU, wbe (16.2)
        SENSOR_ERROR = 0x8004;           // Phone->HU, wbf (16.2)
    }
}
