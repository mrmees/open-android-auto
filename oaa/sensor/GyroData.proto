// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2024-2026 Open Android Auto contributors
// Based on works by: f1x.studio (aasdk), opencardev (crankshaft), aa-proxy (aa-proxy-rs)
syntax="proto2";

package oaa.proto.data;

// confidence: silver [apk_static, cross_version]
message Gyro
{
    optional int32 rotation_speed_x = 1;  // confidence: silver [apk_static, cross_version]
    optional int32 rotation_speed_y = 2;  // confidence: silver [apk_static, cross_version]
    optional int32 rotation_speed_z = 3;  // confidence: silver [apk_static, cross_version]
}
