// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2024-2026 Open Android Auto contributors
// Based on works by: f1x.studio (aasdk), opencardev (crankshaft), aa-proxy (aa-proxy-rs)
syntax="proto2";

import "oaa/sensor/HeadlightStatusEnum.proto";
import "oaa/sensor/IndicatorStatusEnum.proto";

package oaa.proto.data;

// APK class: vxw (AA v16.2) — uses closed enums (zxo.f90982a)
// Note: fields 2,3 default to 1 in APK (unusual — may mean default=ON)
// confidence: silver [apk_static, cross_version]
message Light
{
    optional enums.HeadlightStatus.Enum headlight = 1;  // confidence: silver [apk_static, cross_version]
    optional enums.IndicatorStatus.Enum indicator = 2;  // confidence: silver [apk_static, cross_version]
    optional bool hazard_light_on = 3;  // confidence: silver [apk_static, cross_version]
}
