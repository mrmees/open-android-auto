// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

/*
 * Discovered via APK proto decoder (nll.java, AA v16.1)
 * Google Assistant feature flags — capabilities negotiation.
 * Split from FeatureFlagsData.proto because the APK compiles this as proto3.
 */

syntax="proto3";

package oaa.proto.data;

// APK class: nll — Google Assistant feature flags (14 bool fields)
// confidence: silver [apk_static, cross_version]
message AssistantFeatureFlags
{
    bool assistant_available = 1;  // confidence: silver [apk_static, cross_version]
    bool voice_trigger = 2;  // confidence: silver [apk_static, cross_version]
    bool assistant_shortcuts = 3;  // confidence: silver [apk_static, cross_version]
    bool continued_conversation = 4;  // confidence: silver [apk_static, cross_version]
    bool device_assistant = 5;  // confidence: silver [apk_static, cross_version]
    bool assistant_on_lockscreen = 6;  // confidence: silver [apk_static, cross_version]
    bool smart_display = 7;  // confidence: silver [apk_static, cross_version]
    bool routine_suggestions = 8;  // confidence: silver [apk_static, cross_version]
    bool interpreter_mode = 9;  // confidence: silver [apk_static, cross_version]
    bool multi_modal = 10;  // confidence: silver [apk_static, cross_version]
    bool settings_support = 11;  // confidence: silver [apk_static, cross_version]
    bool field_12 = 12;  // confidence: silver [apk_static, cross_version]
    bool proactive_suggestions = 13;  // confidence: silver [apk_static, cross_version]
    bool field_14 = 14;  // confidence: silver [apk_static, cross_version]
}
