// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2024-2026 Open Android Auto contributors
// Based on works by: f1x.studio (aasdk), opencardev (crankshaft), aa-proxy (aa-proxy-rs)
syntax="proto2";

import "oaa/bluetooth/BluetoothPairingStatusEnum.proto";

package oaa.proto.messages;

message BluetoothPairingResponse
{
    optional bool already_paired = 1;
    optional enums.BluetoothPairingStatus.Enum status = 2;

    // Discovered via APK proto decoder (xgq.java, AA v16.1)
    // APK has this as an enum, not a string
    optional int32 error_code = 3;
}
