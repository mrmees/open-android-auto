// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

/*
 * Bluetooth channel message IDs (service 9, CAR.GAL.BT).
 *
 * Verified 2026-03-07 against qlg.java handler + qav.java sender.
 */

syntax = "proto3";

package oaa.proto.ids;

// confidence: gold [apk_deep_trace]
message BluetoothChannelMessage
{
    // confidence: gold [apk_deep_trace]
    enum Enum
    {
        NONE = 0x0000;
        PAIRING_REQUEST = 0x8001;    // HU→Phone (kba)
        PAIRING_RESPONSE = 0x8002;   // HU→Phone (vvn)
        AUTH_DATA = 0x8003;          // HU→Phone (vvj)
        AUTH_RESULT = 0x8004;        // Phone→HU (vvk) — NEW
    }
}
