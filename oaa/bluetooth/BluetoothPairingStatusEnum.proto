// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

/*
 * RETRACTED (2026-03-07)
 *
 * This enum was WRONG. The actual status field in BluetoothPairingResponse
 * and BluetoothAuthenticationResult uses the shared AA StatusCode enum (vyh),
 * which has 30+ values including BT-specific codes (-10 through -17).
 *
 * The old 3-value enum (NONE=0, OK=1, FAIL=2) was never correct.
 * Both BT response messages now use int32 status field directly.
 *
 * See BluetoothPairingResponseMessage.proto for the relevant status values.
 *
 * This file is kept as a tombstone to prevent re-introduction.
 */

syntax = "proto2";

package oaa.proto.enums;

// RETRACTED — do not use
// message BluetoothPairingStatus { ... }
