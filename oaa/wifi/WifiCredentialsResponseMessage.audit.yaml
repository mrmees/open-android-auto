proto: oaa/wifi/WifiCredentialsResponseMessage.proto
message: WifiCredentialsResponse
confidence: gold
last_updated: '2026-03-07'
evidence:
- type: deep_trace
  method: handler_trace
  source: ibr.java (16.2) — GAL handler for CAR.GAL.WIFI_PROJ (service 17)
  date: '2026-03-07'
  description: >
    Only message on WiFi Projection GAL channel. Handler at ibr.mo18864a()
    checks msg ID == 0x8002 (32770), parses wcw proto. Fields traced through
    CarInfoInternal storage: field 1 (string, f75249b), field 2 (enum via
    C0000a.m129bx, 12 values 0-11), field 3 (string, f75251d), field 5
    (enum via C0000a.m91bL, 2 values 0-1). Field names inferred from
    CarInfoInternal storage order and analogy with BT RFCOMM WifiSecurityResponse.
    Security mode uses sequential re-indexing (not WifiSecurityMode non-sequential values).
    Status field: 0=success (store creds), 1=failure (discard).
