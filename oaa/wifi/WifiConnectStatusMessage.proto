// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

/*
 * WifiConnectStatus — WiFi connection status update (DEPRECATED).
 *
 * APK class: wdj.java (AA v16.1) — marked @Deprecated
 * BT RFCOMM message type: ordinal 5 (MESSAGE_WIFI_CONNECT_STATUS_BT)
 */

syntax="proto2";

package oaa.proto.messages;

// Deprecated in APK — kept for protocol completeness.
// NOTE: state uses the same WifiVersionStatus sint32 wire encoding
// with negative values. Cannot express as proto enum — see WifiVersionStatusEnum.proto.
message WifiConnectStatus {
    optional sint32 state = 1;          // APK: b (int, enum via wck.p/viz.au — WifiVersionStatus)
    optional string status_text = 2;    // APK: d (String) — descriptor says string
}
