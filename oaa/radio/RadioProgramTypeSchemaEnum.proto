// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

/*
 * RadioProgramTypeSchema — classification scheme for radio program types.
 *
 * APK verifier: viz.M() (AA v16.1) — accepts values 1, 2.
 * Used in RadioProgramType field 1.
 */

syntax="proto2";

package oaa.proto.enums;

message RadioProgramTypeSchema
{
    enum Enum
    {
        SCHEMA_UNKNOWN = 0;
        SCHEMA_RDS_PTY = 1;
        SCHEMA_ITU = 2;
    }
}
