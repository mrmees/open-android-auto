// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

/*
 * TouchPadConfig — touch pad capability descriptor (vxy.java, AA v16.1).
 *
 * Used as repeated field in InputChannel (vya) field 3.
 * Extended config with 8 fields for trackpad/touchpad devices.
 */

syntax="proto3";

package oaa.proto.data;

// confidence: unverified
message TouchPadConfig
{
    optional int32 width = 1;  // confidence: unverified
    optional int32 height = 2;  // confidence: unverified
    optional bool unknown_3 = 3;  // confidence: unverified
    optional int32 unknown_4 = 4;  // confidence: unverified
    optional int32 unknown_5 = 5;  // confidence: unverified
    optional bool unknown_6 = 6;  // confidence: unverified
    optional bool unknown_7 = 7;  // confidence: unverified
    optional int32 sensitivity = 8; // APK toString (qmr.java): "sensitivity"  // confidence: unverified
}
