// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

/*
 * TouchScreenConfig — touch screen capability descriptor (vxz.java, AA v16.1).
 *
 * Used as repeated field in InputChannel (vya) field 2.
 * Contains display dimensions and display type enum.
 */

syntax="proto3";

import "oaa/video/DisplayTypeEnum.proto";

package oaa.proto.data;

// confidence: unverified
message TouchScreenConfig
{
    optional int32 width = 1;  // confidence: unverified
    optional int32 height = 2;  // confidence: unverified
    optional enums.DisplayType.Enum display_type = 3;  // confidence: unverified
}
