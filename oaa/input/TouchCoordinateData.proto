// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

/*
 * Discovered via APK proto decoder (wbl.java, AA v16.1)
 * Touch coordinate pair used in touch events.
 */

syntax="proto2";

package oaa.proto.data;

// APK class: wbl — touch coordinate
// confidence: silver [apk_static, cross_version]
message TouchCoordinate
{
    optional uint32 x = 1;  // confidence: silver [apk_static, cross_version]
    optional int32 y = 2;  // confidence: silver [apk_static, cross_version]
    optional uint32 pointer_id = 3;  // confidence: silver [apk_static, cross_version]
}
