// Android Auto Protocol Buffer Definitions
// https://github.com/mrmees/open-android-auto
// SPDX-License-Identifier: GPL-3.0-or-later

// RETRACTED: these classes are Google Surveys request-context protos, not
// Android Auto capability negotiation. Retained as research history only.

/*
 * Phone capability sub-messages — proto3 types used in ServiceDiscoveryRequest.
 * Split from PhoneCapabilitiesData.proto because the APK compiles these as proto3.
 */

syntax="proto3";

package oaa.proto.data;

// APK class: aafn (16.2), aagh (16.1) — boolean flag + name
// confidence: retracted [apk_deep_trace, apk_static, cross_version]
message CapabilityFlag
{
    bool enabled = 1;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    string name = 2;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
}

// APK class: aafy (16.2), aags (16.1) — boolean pair
// confidence: retracted [apk_deep_trace, apk_static, cross_version]
message CapabilityPair
{
    bool value_a = 1;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    bool value_b = 2;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
}

// APK class: aaez (16.2), aaft (16.1) — paired ping configurations
// confidence: retracted [apk_deep_trace, apk_static, cross_version]
message PingConfigPair
{
    PingConfigEntry phone_to_hu = 1;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    PingConfigEntry hu_to_phone = 2;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
}

// APK class: zxk (16.2) — ping configuration entry
// 16.2 has only 2 fields (int64 + int32), NOT the 4 uint32 fields previously assumed
// confidence: retracted [apk_deep_trace, apk_static, cross_version]
message PingConfigEntry
{
    int64 timeout_ms = 1; // 16.2: long (was uint32)  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    int32 interval_ms = 2; // 16.2: int (was uint32)  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    // fields 3-4 (high_latency_threshold_ms, tracked_ping_count) NOT in 16.2 APK — removed
}

// APK class: aagb (16.2), aagv (16.1) — individual capability entry (10 fields)
// Fields 4-7 are oneof (capability_value)
// confidence: retracted [apk_deep_trace, apk_static, cross_version]
message CapabilityEntry
{
    uint32 capability_id = 1;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    string capability_name = 2;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    CapabilityEnum capability_type = 3;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    oneof capability_value {
        CapabilityValueMsg value_msg_4 = 4; // 16.2: aagk  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
        CapabilityValueMsg value_msg_5 = 5; // 16.2: aafv  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
        CapabilityValueMsg value_msg_6 = 6; // 16.2: aagc  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
        CapabilityValueMsg value_msg_7 = 7; // 16.2: aafw  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    }
    bool enabled = 8;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    string description = 9;  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
    repeated CapabilityValueMsg sub_entries = 10; // 16.2: aagq  // confidence: retracted [apk_deep_trace, apk_static, cross_version]
}

// Placeholder for CapabilityEntry oneof values (sub-message type TBD)
// TODO: oneof variants have distinct 16.2 classes (aagk, aafv, aagc, aafw) — may have different schemas
// confidence: retracted [apk_deep_trace, apk_static, cross_version]
message CapabilityValueMsg {}

// Placeholder enum for capability type (field 3 of CapabilityEntry)
// confidence: retracted [apk_deep_trace, apk_static, cross_version]
enum CapabilityEnum {
    CAPABILITY_UNKNOWN = 0;
}
