# 06-capture-non-claim-boundary.md FIXTURE (simulates self-exclusion target)

This is a fixture for the self-exclusion test. The walker MUST NOT touch this file
even though it contains the sentinel below (the sentinel is the real doc's content,
not a cross-link callout):

> **Capture evidence boundary:** The VW capture cannot validate claims about this surface.
> This fixture exists to verify the walker's self-exclusion logic works.
